function goEdit(element) {
    alert(element);
}

/*
    funcion que haga window.location.href = "/edit?id="element.id;
*/

window.addEventListener('load', hazAlgo);
function hazAlgo() {
    console.log('funciona el script!!');
}